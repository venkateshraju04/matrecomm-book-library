services:
  - type: web
    name: book-realtime-app
    runtime: node
    rootDir: book-realtime-app
    buildCommand: cd frontend && npm install && npm run build && cd ../backend && npm install --omit=dev
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
